fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-dotenv>=1.0
sse-starlette>=2.0
cryptography>=42.0

# Motor de download do SAT Central (Playwright headed via CDP). Versao FIXA
# (nao ">=") de proposito: cada versao do playwright baixa um build proprio de
# Chromium (pasta ms-playwright/chromium-XXXX). Usar a MESMA versao do
# PAINEL SISDPU faz reaproveitar o mesmo chrome.exe ja liberado no antivirus/
# firewall corporativo (Fortinet) — uma versao diferente baixa um binario novo,
# nunca visto, que pode ser bloqueado ate ser aprovado pelo TI.
playwright==1.60.0
pymupdf>=1.23
